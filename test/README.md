# テスト

## class.jsのテスト

直接 `class_test.html` をchromeで開いてください。

## content.jsのテスト

`content_test.php` を実行するためには、一つ上の階層でビルトインウェブサーバーを起動してください。  
`content_test.php` は実際のメンターのレビューページのHTMLが入っているため、GitHubで非公開にします。

### For example.

```
php -S 127.0.0.1:8080
```